'use strict';

import React from 'react';
import theme from '../theme';

let Excerpt = React.createClass({
  render() {
    return (
      <span
        className="Excerpt"
      />
    );
  }
});

export default Excerpt;
